<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/CKButton</item>
    </style>
<style name="materialaa" parent="Theme.AppCompat.Light.DarkActionBar">
    <item name="colorPrimary">@color/primary500</item>
    <item name="colorPrimaryDark">@color/primary700</item>
    <item name="colorAccent">@color/accent500</item>
    <item name="colorButtonNormal">@color/primary500</item>


</style>
    <style name="CKButton" parent="android:style/Widget.Button">
        <item name="android:textSize">35dp</item>
        <item name="android:layout_margin">0dip</item>
        <item name="android:height">50dp</item>
        <item name="android:background">@color/primary500</item>
        <item name="android:textColor">@color/primary50</item>
    </style>
</resources>
